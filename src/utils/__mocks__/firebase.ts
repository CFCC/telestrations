export const firebaseLoginUi = {
    start: jest.fn(),
};

export const playerRef = (gameCode: string, playerId: string) => {

};

export const playerListRef = (gameCode: string) => {

};

export const notebookRef = (gameCode: string, notepadId: string) => {

};

export const notebookListRef = (gameCode: string) => {

};

export const gameListRef = () => {

};

export const gameRef = (gameCode: string) => {

};

export const storageRef = (fileName: string) => {

};
